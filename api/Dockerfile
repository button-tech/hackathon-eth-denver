FROM node:9

WORKDIR /app

COPY . .

RUN npm install

ENV BOT_TOKEN=
ENV URL=
ENV LOGIN=
ENV PASSWORD=
ENV DB=
ENV INFURA_TOKEN=
ENV REDIS_HOST=

EXPOSE 3000
EXPOSE 6379
EXPOSE 27017

ENTRYPOINT ["node", "api.js"]